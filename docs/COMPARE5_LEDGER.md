# nightfall!punk — Compare-5 research ledger

Purpose: keep research cumulative without rewriting the same comparison essay every build. A reference owns a mechanism only when it contributes a distinct transferable lesson. Later references cite the owning shorthand and record only their unique delta.

## Core syntax

Use:

`C5:<REF>/<MECH>/<DISPOSITION>`

Examples:

- `C5:RW/REC/A` = adapt Rain World recurrence lesson.
- `C5:AL/LOD/A` = adapt A-Life simulation-resolution lesson.
- `C5:PX/WATER/R` = reject Prototype's excessive presentation for the waterline while retaining another PX lesson separately.

Dispositions:

- `K` = KEEP: nightfall already has the right mechanism; protect it.
- `A` = ADAPT: transferable mechanism needs a bounded nightfall implementation.
- `D` = DEFER: useful, but not earned by current evidence/build.
- `R` = REJECT: conflicts with architecture, tone, causal truth, or complexity budget.

Evidence shorthand:

- `OBS` = observed in the reference.
- `EQ` = existing nightfall equivalent.
- `GAP` = current discrepancy or missing relation.
- `CODE` = smallest coding consequence.
- `VID` = next human-video proof.

## Reference IDs — initial v1.1 set

| ID | Reference | Owns / primary unique use | Do not repeatedly re-explain |
|---|---|---|---|
| `RW` | Rain World | creature absence/return, habitat recurrence, local embodiment | generic emergent-AI praise |
| `AL` | S.T.A.L.K.E.R. / A-Life | regional/offscreen causal continuity, autonomous travel, simulation LOD | generic open-world AI |
| `AI` | Alien: Isolation | stalking/haunting, absence-as-tension, readable threat ecology | generic horror atmosphere |
| `PR` | Prey (2017) | hostile materiality, environmental uncertainty, architecture made ecologically uncanny | generic immersive-sim feature lists |
| `PX` | Prototype | immediate territorial/infection phenotype and readable takeover | gore, arcade excess, power-fantasy scale |

Secondary aesthetic shorthand, not default Compare-5 owners:

- `KH` = Kingdom Hearts / Heartless — shadow-material uncanniness.
- `LOST` = Smoke Monster from Lost — amorphous environmental stalking / smoky presence.

Use secondary refs only when they add a visual delta not already owned by `AI`, `PR`, or `PX`.

## Supporting non-game evidence

Supporting evidence may confirm or sharpen a mechanism without becoming a sixth Compare-5 owner. Record it as `SUP:<REF>/<MECH>/<DISPOSITION>` and map it back to an existing C5 mechanism or current ledger contract.

- `SUP:PHYS/FIELD/K` — *Physarum* / slime-mold + sugar chemotaxis supports the distinction between uniform opportunity, stochastic exploration, and **conditional field-biased movement**. Keep the mechanism; do not imitate the organism literally.
- `SUP:PHYS/TRACE/A` — slime-mold environmental history supports the idea that prior occupation or residue can alter later probability. Adapt only as bounded, causal, decaying world history already compatible with `TRACE`, Q8 primary/secondary persistence, Q10 situated selection, and the v1.0 stochastic-lifeworld addendum.

Supporting biological abstraction:

`uniform tick -> stochastic proposal -> local gradient bias -> feedback -> historical trace -> conditional recurrence`

Interpretation for nightfall:

- **Uniform is WHEN:** authoritative fixed-step opportunity.
- **Random is MAYBE:** seeded contingency among plausible alternatives.
- **Cluster is BECAUSE:** conditional recurrence produced by prior world/actor relations, not a third independent random sampler.
- **Gradient is WHAT IS HERE:** local resources/hazards/social evidence bias behavior without omniscience.
- **Memory is WHAT WAS HERE:** bounded history changes future probability.

Do not create a general slime-mold simulation, cellular automaton, pheromone field, or universal behavior vector merely because the analogy exists. Any coding consequence must remain the smallest evidence-backed change to current nightfall systems.

## Mechanism tags

| Tag | Mechanism |
|---|---|
| `REC` | ecological recurrence / absence / return |
| `LOD` | simulation-resolution changes while causality persists |
| `HAUNT` | presence/absence tension and stalking habitat |
| `WATER` | perceptual ecological waterline |
| `TRACE` | persistent material residue / historical evidence |
| `TOPO` | topography materially changes behavior/value |
| `INFO` | situated knowledge / evidence / uncertainty |
| `ATTN` | bounded attention / information acquisition |
| `MET` | metabolism, stocks/flows, maintenance, infrastructure |
| `EVENT` | consequence becomes cause / structural transition |
| `COMBAT` | direct confrontation remains readable inside systemic life |
| `SOC` | cooperation/competition/social ecology |
| `PERSIST` | exact primary history / round or offscreen continuity |
| `PRES` | presentation-only lesson; must remain downstream of causal state |
| `FIELD` | local environmental gradient biases otherwise stochastic behavior |

## Redundancy rule

If a mechanism already has an owner, later references must cite it instead of repeating it.

Example:

`PR` also has stalking atmosphere, but if `AI/HAUNT` already owns the behavioral lesson, record:

`PR/HAUNT = AI/HAUNT; delta = alien materiality changes architecture itself.`

Likewise:

`PX/WATER = PR/WATER; delta = stronger at-a-glance territorial legibility; reject gore/arcade exaggeration.`

A Compare-5 row is useful only if it changes one of:

1. a coding contract;
2. a protected architectural boundary;
3. a human-proof question;
4. a deliberate rejection/defer decision.

Otherwise mark it `CONFIRM` in notes and do not create work.

## Canonical row schema

| C5 ID | Unique mechanism observed | Current nightfall equivalent | GAP | K/A/D/R | Smallest CODE consequence | VID proof | Notes |
|---|---|---|---|---|---|---|---|
| `C5:RW/REC/A` | habitat-linked absence and return | v1.0 Cattler ecological recurrence | evidence after completion demo | `A` | only if video shows recurrence still feels like spawn logic | player sees real absence then later physical return | owner of `REC` |
| `C5:AL/LOD/A` | actors/world continue at lower resolution | C6 regional/global causal fidelity | no round-load persistence yet | `A` | determine minimum offscreen state before JSON restore | leave/re-enter locality without reroll | owner of `LOD` |
| `C5:AI/HAUNT/A` | threat is legible through absence and environmental anticipation | v1.0 haunted Cattler habitat | calibrate tension vs combat clarity | `A` | tune only evidence-supported waterline channels | player infers habitat before seeing Cattler | owner of `HAUNT` |
| `C5:PR/WATER/A` | hostile materiality makes architecture uncertain | blue-black smoke/static/synthetic residue | graybox phenotype needs human read | `A` | preserve geometry-bound, causal presentation | infestation distinct from ordinary black geometry | owner of uncanny materiality |
| `C5:PX/PRES/A` | takeover is readable immediately at territorial scale | infestation darkening | avoid gore/arcade spectacle | `A` | increase legibility without aura/HUD | player points out occupied locality at first glance | owner of strong takeover legibility |

## Compare-5 subroutine

`WATCH/READ -> QUESTION -> C5 TAG -> COMPARE CURRENT CODE -> K/A/D/R -> SMALLEST CODE CONSEQUENCE -> COMPILE -> HUMAN VIDEO -> REASSESS`

Before accepting a Compare-5 lesson ask:

1. What unique mechanism does this source contribute that no prior shorthand already owns?
2. Is the lesson causal/behavioral, perceptual, or merely aesthetic?
3. What already exists in nightfall that solves most of it?
4. What is the smallest actual gap?
5. Can the next recording prove/disprove that gap?
6. What would we delete or avoid if the source merely confirms existing architecture?

## Dialectical epistemic gate

All consequential Compare-5 work also inherits `docs/DIALECTICAL_EPISTEMIC_PROMPTING.md`.

Use `DEP:0-INF-A-N-S` before promoting a comparison into a design contract:

1. **0 / ZERO:** state the underlying problem without assuming FPS convention.
2. **INF / MAX-PRECEDENT:** reconstruct the strongest relevant genre/subgenre precedent and alternatives.
3. **A / ACTUAL:** identify the present nightfall implementation/evidence, not the desired one.
4. **N / NEGATE:** challenge the premise, analogy, terminology and novelty claim.
5. **S / SYNTHESIZE:** accept only the smallest testable delta.

Every comparison should explicitly preserve both similarity and difference. Similarity-only comparison risks imitation; difference-only comparison risks novelty theater.

Dependency rule: when a reference identity, premise or interpretation is corrected, re-evaluate downstream `EQ`, `GAP`, disposition and CODE consequences. A corrected premise cannot be repaired by word substitution alone.

Status rule: `ANALOGY != OBSERVED`, `ASPIRATION != IMPLEMENTED`, and `COHERENT PROSE != PROOF`.

## v1.1 start rule

v1.1 begins as a research/question branch, not a feature dump.

1. Review the accepted v1.0 video/log evidence.
2. Run the five reference passes using this ledger.
3. Record only unique `GAP` rows.
4. Ask the first bounded questions.
5. Freeze a v1.1 contract only after the questions and references agree on a smallest next proof.

Maxim: **Compare mechanisms, not feature inventories. Record deltas, not repeated essays.**
