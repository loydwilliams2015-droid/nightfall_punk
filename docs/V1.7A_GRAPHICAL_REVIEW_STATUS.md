# v1.7A graphical review status checkpoint

Implementation present on `build/v1.7a-spatial-logic-laboratory`:
- bounded instrumented and perceptual review modes;
- greedy and random controls;
- deterministic trace contract;
- canonical seed selector;
- 320-row selected-seed review batch;
- raylib referee viewer with 40 side-by-side frames;
- OITOA / soil / PAR / correspondence analyzer;
- CI workflow wiring and evidence upload.

Pending status at this checkpoint: exact-head hosted CI confirmation and native human inspection of selected graphical cases.
